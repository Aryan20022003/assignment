const CustomButton = ({text}) => {
    return (
        <button className="bg-button text-white font-semibold py-3 px-4 rounded-full w-full mt-auto shadow-md">
            {text}
        </button>
    );
};

export default CustomButton